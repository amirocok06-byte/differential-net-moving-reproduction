# Manifest contents

These are small JSON metadata files copied from local experiments. They preserve design/method/provenance fields and selected scalar metrics, but no tensor/checkpoint files are included. They are not a complete result table and do not establish paper-level comparability.
