# Prototype overlay

The Python files under `dreamplace/` are the local DiffNet research layer. They are intentionally not a full `dreamplace` package: imports such as `dreamplace.PlaceObj` resolve only when this overlay is combined with a compatible upstream DREAMPlace checkout.

The code contains paper-aligned probes and reconstruction assumptions. It does not establish exact author-code equivalence.
